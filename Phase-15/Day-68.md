# 📅 Day 68: REST API Practice — Library Management API

## 🎯 Goal of the Day
Build a complete Library Management REST API.

## API Endpoints
- GET /api/books — List all books (with pagination)
- GET /api/books/{id} — Get book by ID
- POST /api/books — Add a new book
- PUT /api/books/{id} — Update a book
- DELETE /api/books/{id} — Delete a book
- GET /api/books/search?title=xxx — Search books
- POST /api/books/{id}/borrow — Borrow a book
- POST /api/books/{id}/return — Return a book

## ✅ End of Day Checklist — Phase 15 Complete!
- [ ] Library API is working
- [ ] APIs are documented with Swagger
- [ ] Ready for Phase 16: JPA + Hibernate
